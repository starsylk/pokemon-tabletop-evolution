{
  "name": "pte",
  "version": "2.0.0",
  "description": "CSS compiler for the PTE system",
  "scripts": {
    "build": "sass src/scss/pte.scss css/pte.css --style=expanded --no-source-map",
    "watch": "sass src/scss/pte.scss css/pte.css --style=expanded --source-map --watch"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "author": "Asacolips",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "sass": "^1.53.0"
  }
}